= hidden_field_tag 'return_value', ''
= submit_tag "Export PDF",:formtarget => "_blank", onClick: "$(this).closest('form').attr('action', '#{ deplacements_export_path(format: :pdf) }')", data: {confirm: 'Are you sure? you can only export once !',disable_with: false}, class:'btn btn-primary submitexportbtn'
= submit_tag "Export CSV",:formtarget => "_blank", onClick: "$(this).closest('form').attr('action', '#{ deplacements_export_path(format: :csv) }')", data: {confirm: 'Are you sure? you can only export once !',disable_with: false}, class:'btn btn-primary submitexportbtn'